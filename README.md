# PHP next WebAssembly builds

Generated from php-src ref `master` at PHP version `8.6.0-dev` for modes: jspi, asyncify.

These artifacts are published from the `refresh-php-next.yml` workflow and consumed by playground.wordpress.net.
