# Custom Node.js module loader

This custom Node.js module loader allows us to run Playground quickly with Node.js versions that support type stripping.

The source files are given the extension .mts to avoid Node.js warnings about reparsing after detecting the files are ES modules.
