daemon off;
master_process on;
worker_processes 1;
error_log stderr info;
pid /tmp/nginx.pid;

events {
    worker_connections 64;
    use poll;
}

http {
    access_log /dev/stderr;

    client_body_temp_path /tmp/nginx_client_temp;

    # Every request goes to PHP-FPM via the router script. The router
    # serves static files directly with the right Content-Type and
    # routes PHP / WordPress requests through index.php. This avoids
    # needing PCRE in the nginx build.
    server {
        listen __PORT__;
        server_name __SERVER_NAME__;

        root __WORDPRESS_ROOT__;

        location / {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_param SCRIPT_FILENAME __ROUTER_SCRIPT__;
            fastcgi_param DOCUMENT_ROOT $document_root;
            fastcgi_param DOCUMENT_URI $document_uri;
            fastcgi_param QUERY_STRING $query_string;
            fastcgi_param REQUEST_METHOD $request_method;
            fastcgi_param CONTENT_TYPE $content_type;
            fastcgi_param CONTENT_LENGTH $content_length;
            fastcgi_param REQUEST_URI $request_uri;
            fastcgi_param SERVER_PROTOCOL $server_protocol;
            fastcgi_param SERVER_PORT $server_port;
            fastcgi_param SERVER_NAME $server_name;
            fastcgi_param HTTP_HOST $http_host;
            fastcgi_param REDIRECT_STATUS 200;
            fastcgi_param PLAYGROUND_FIRST_REQUEST_MARKER __FIRST_REQUEST_MARKER__;
        }
    }
}
