import { playgroundPhpBridge } from './playground-php-bridge';

describe('playgroundPhpBridge', () => {
	it('should work', () => {
		expect(playgroundPhpBridge()).toEqual('playground-php-bridge');
	});
});
