---
title: Create a one-click bug or pull-request reproduction
slug: /blueprints/v2/tutorials/bug-reproduction
description: Share a pinned WordPress test case that runs in a browser, locally, and in CI with Blueprint v2.
---

# Create a one-click bug or pull-request reproduction

A useful reproduction gives every reviewer the same runtime, code, data,
starting URL, and steps to follow. Blueprint v2 can open that test case with one
click in a browser, while the same committed file runs headlessly in CI.

## What you will build

- Exact WordPress and plugin releases on a selected PHP runtime line.
- Minimal content that states one expected and one actual result.
- Short, numbered steps another person can follow without extra context.
- A visible environment marker generated by a small inline mu-plugin.
- A share URL that starts the example directly in Playground.
- A headless command suitable for a pull request or continuous integration.

## Prerequisites

- A minimal sequence that reproduces the behavior on a fresh site.
- Exact released versions, or the full commit SHA of code under review.
- A public, CORS-readable URL if reviewers will load the JSON in a browser.
- Node.js for local and CI execution with the Playground CLI.

## Run the finished example

[Run in Playground](https://playground.wordpress.net/?blueprint-url=https://raw.githubusercontent.com/WordPress/wordpress-playground/refs/heads/trunk/packages/docs/site/src/examples/blueprints-v2/bug-reproduction.json)

The example opens `/bug-reproduction/` and reports WordPress 6.8.3, the PHP 8.3
runtime line, and Hello Dolly 1.7.2 as active. Its report page includes **Steps
to reproduce**, **Expected**, and **Actual** sections. Replace that sample report
and dependency with the smallest state that exposes the real problem. After it
opens, use **Dock → Blueprint** to inspect or edit the JSON.

<details>

<summary>View and copy the complete Blueprint</summary>

```json blueprint-v2 fixture=bug-reproduction
{
	"$schema": "https://playground.wordpress.net/blueprint-schema.json",
	"version": 2,
	"blueprintMeta": {
		"name": "Bug reproduction",
		"description": "A pinned, one-click environment for reporting a WordPress bug or reviewing a pull request.",
		"authors": ["WordPress Playground"],
		"tags": ["bug", "reproduction", "testing"]
	},
	"applicationOptions": {
		"wordpress-playground": {
			"landingPage": "/bug-reproduction/",
			"login": false,
			"networkAccess": false
		}
	},
	"phpVersion": "8.3",
	"wordpressVersion": "6.8.3",
	"constants": {
		"WP_DEBUG": true,
		"WP_DEBUG_DISPLAY": true,
		"SCRIPT_DEBUG": true
	},
	"plugins": [
		{
			"source": "hello-dolly@1.7.2",
			"active": true,
			"ifAlreadyInstalled": "skip"
		}
	],
	"muPlugins": [
		{
			"filename": "reproduction-environment.php",
			"content": "<?php\n/**\n * Plugin Name: Reproduction environment\n */\nadd_shortcode( 'repro_environment', function () {\n\t$hello_dolly = get_file_data(\n\t\tWP_PLUGIN_DIR . '/hello-dolly/hello.php',\n\t\tarray( 'Version' => 'Version' )\n\t);\n\t$hello_dolly_status = in_array(\n\t\t'hello-dolly/hello.php',\n\t\t(array) get_option( 'active_plugins', array() ),\n\t\ttrue\n\t) ? 'active' : 'inactive';\n\n\treturn '<dl><dt>WordPress</dt><dd>' . esc_html( get_bloginfo( 'version' ) ) . '</dd><dt>PHP</dt><dd>' . esc_html( PHP_MAJOR_VERSION . '.' . PHP_MINOR_VERSION ) . '</dd><dt>Hello Dolly</dt><dd>' . esc_html( $hello_dolly['Version'] . ' (' . $hello_dolly_status . ')' ) . '</dd></dl>';\n} );\n"
		}
	],
	"siteOptions": {
		"blogname": "Bug reproduction",
		"permalink_structure": "/%postname%/"
	},
	"content": [
		{
			"type": "posts",
			"source": {
				"post_title": "Bug reproduction",
				"post_name": "bug-reproduction",
				"post_type": "page",
				"post_status": "publish",
				"post_content": "<!-- wp:heading {\"level\":1} --><h1 class=\"wp-block-heading\">Bug reproduction</h1><!-- /wp:heading --><!-- wp:shortcode -->[repro_environment]<!-- /wp:shortcode --><!-- wp:heading {\"level\":2} --><h2 class=\"wp-block-heading\">Steps to reproduce</h2><!-- /wp:heading --><!-- wp:list {\"ordered\":true} --><ol class=\"wp-block-list\"><!-- wp:list-item --><li>Read the WordPress, PHP, and Hello Dolly versions shown above.</li><!-- /wp:list-item --><!-- wp:list-item --><li>Confirm Hello Dolly is active and compare all three values with the Expected section.</li><!-- /wp:list-item --></ol><!-- /wp:list --><!-- wp:columns --><div class=\"wp-block-columns\"><!-- wp:column --><div class=\"wp-block-column\"><!-- wp:heading {\"level\":2} --><h2 class=\"wp-block-heading\">Expected</h2><!-- /wp:heading --><!-- wp:paragraph --><p>The page reports WordPress 6.8.3, PHP 8.3, and Hello Dolly 1.7.2 (active).</p><!-- /wp:paragraph --></div><!-- /wp:column --><!-- wp:column --><div class=\"wp-block-column\"><!-- wp:heading {\"level\":2} --><h2 class=\"wp-block-heading\">Actual</h2><!-- /wp:heading --><!-- wp:paragraph --><p>The environment table above shows the versions and plugin state loaded by this run.</p><!-- /wp:paragraph --></div><!-- /wp:column --></div><!-- /wp:columns -->"
			}
		}
	]
}
```

</details>

## Reproduce the example

This sample checks that the selected runtimes and plugin are the inputs you
actually receive:

1. Open **Run in Playground** in a fresh browser tab.
2. Wait for `/bug-reproduction/` to load.
3. Under the page title, read the reported WordPress, PHP, and Hello Dolly
   versions and the plugin's activation state.
4. Compare them with the expected WordPress 6.8.3, PHP 8.3, and active Hello
   Dolly 1.7.2 values.
5. Confirm the page contains **Steps to reproduce**, **Expected**, and
   **Actual** sections.

For a real bug, replace that sequence with the shortest interaction that
exposes the problem. Include those numbered steps in the issue as well as in
the reproduction page whenever possible.

## Customize the example

| Example field                    | Replace it with                                          | Check after a clean run                              |
| -------------------------------- | -------------------------------------------------------- | ---------------------------------------------------- |
| `wordpressVersion`, `phpVersion` | The exact WordPress release and failing PHP runtime line | The page reports the intended versions               |
| `plugins`, `activeTheme`         | Only the code required to reproduce the behavior         | Every remaining item changes or exposes the result   |
| `constants`                      | Debug settings needed to observe the failure             | Useful errors are visible without exposing secrets   |
| `content`                        | Test data plus steps, expected result, and actual result | A reviewer knows what to do and what to look for     |
| `landingPage`                    | The closest stable URL to the failing screen             | The first manual step starts from that screen        |
| `muPlugins`                      | Small setup code that site fields cannot express         | The inline PHP is short enough to review in the JSON |

Make the report page follow the same order a reviewer will use: **Steps to
reproduce**, **Expected**, then **Actual**. Keep each step observable and remove
any step that does not contribute to the result.

## 1. Pin every input that defines the result

The example uses exact WordPress and plugin releases, plus a selected PHP
runtime line:

```jsonc
"phpVersion": "8.3",
"wordpressVersion": "6.8.3",
"plugins": [
	{
		"source": "hello-dolly@1.7.2",
		"active": true,
		"ifAlreadyInstalled": "skip"
	}
]
```

Do not use `latest`, a moving branch, or an unversioned WordPress.org slug in a
long-lived bug report. Those inputs can change without changing the JSON.

For a plugin or theme built directly from Git, use the full commit SHA:

```jsonc
"plugins": [
	{
		"source": {
			"gitRepository": "https://github.com/acme/example-plugin",
			"ref": "4f86d3f04b47e86a9802a58e6c7aef2f3cb92f55",
			"pathInRepository": "plugin"
		},
		"targetDirectoryName": "example-plugin",
		"active": true
	}
]
```

A pull-request ref is convenient while a review is moving, but it is mutable.
Record the full head commit in a reproduction that must still mean the same
thing after the pull request changes or closes. Git sources install committed
files only; point to a built artifact when the project requires a build step.

## 2. Declare only representative state

The example turns on debug constants, creates one page, and registers one
inline shortcode. This is enough to make the runtime visible and keep the
expected-versus-actual report beside the test content.

For a real issue:

1. Remove unrelated plugins, themes, options, users, and content.
2. Preserve the exact activation state that triggers the problem.
3. Use stable slugs and an explicit `landingPage`.
4. State one observable expected result and one observable actual result.
5. Keep any interaction after boot short enough to repeat in a review.

The inline mu-plugin is executable PHP. Use it only for setup that cannot be
expressed as ordinary site state, and keep it small enough to review in the
JSON. Use [final setup steps](/blueprints/v2/reference/additional-steps) only
when the reproduction genuinely needs command-like work after the top-level
site fields.

## 3. Commit the example and make a browser share URL

Host the JSON at an immutable URL. A raw GitHub URL should contain the full
commit SHA, not `main` or `trunk`. This snippet prints the share URL so you can
open or paste it:

```js
const commit = '4f86d3f04b47e86a9802a58e6c7aef2f3cb92f55';
const blueprintUrl = ['https://raw.githubusercontent.com/acme/example-plugin', commit, 'tests/bug-reproduction.json'].join('/');

const shareUrl = new URL('https://playground.wordpress.net/');
shareUrl.searchParams.set('blueprint-url', blueprintUrl);
console.log(shareUrl.href);
```

Open `shareUrl` in a signed-out browser before posting it. The JSON and every
adjacent remote asset need normal browser fetch and CORS access. For private or
binary inputs, prefer an attached, reviewable bundle rather than embedding
credentials in a URL.

## 4. Run the same example headlessly

Execute it locally without starting a web server:

```bash
npx @wp-playground/cli@latest run-blueprint \
	--blueprint=./tests/bug-reproduction.json
```

For CI, install the CLI as a development dependency and commit the resulting
lockfile. That pins the runner version used by future checks:

```bash
npm install --save-dev --save-exact @wp-playground/cli
npm exec -- wp-playground-cli run-blueprint \
	--blueprint=./tests/bug-reproduction.json
```

Then run the same command after `npm ci`. For example:

```yaml
name: Blueprint reproduction

on:
    pull_request:

jobs:
    run-blueprint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: npm
            - run: npm ci
            - run: >-
                  npm exec -- wp-playground-cli run-blueprint
                  --blueprint=./tests/bug-reproduction.json
```

The command checks schema validation, source resolution, installation, and
setup. Keep assertions about the bug's visible behavior in a separate test so
the Blueprint remains a readable description of the test site.

Recommended CI example rules:

- Commit `blueprint.json` and every local input it references.
- Use exact WordPress.org versions and full Git commit SHAs.
- Grant `--blueprint-may-read-adjacent-files` when a local JSON file
  intentionally reads sibling files. A directory input is shorthand for that
  JSON file and needs the same flag; a ZIP bundle does not.
- Let the CLI validate the Blueprint before application-specific assertions.
- Avoid network-dependent setup unless network behavior is itself under test.
- Update a pin in a reviewable commit rather than silently following a branch.

See [Run Blueprints v2](/blueprints/v2/blueprints-101/run) for browser, CLI,
and JavaScript entry points.

## You are done when

- The browser link loads without credentials or repository access.
- The page states the steps, expected result, and actual result.
- WordPress and dependency releases are pinned, the PHP runtime line is
  selected, and every Git source uses a full commit SHA.
- Another person can reproduce the behavior from a fresh site without asking
  what to click.
- The locked CLI runs the same Blueprint successfully in CI.

## Security and failure reporting

A reproduction can install and execute arbitrary plugin, theme, mu-plugin, PHP,
SQL, or WP-CLI code. Review the example before running it, use a temporary site,
and never store secrets in JSON or a share URL. The example keeps
`networkAccess: false`; enable it only when outbound access is essential to the
bug.

Blueprint execution is not transactional. If it fails halfway through, report
the failing phase and source path, then retry on a fresh site. The
[troubleshooting guide](/blueprints/v2/troubleshooting) follows the parse,
validate, resolve, and execute phases in order.

## Related guides

- [Blueprint v2 quickstart](/blueprints/v2/blueprints-101/get-started)
- [Run and share Blueprints v2](/blueprints/v2/blueprints-101/run)
- [Pin remote, Git, and bundled sources](/blueprints/v2/resources)
- [Security and reproducibility](/blueprints/v2/security)
- [Blueprint v2 schema reference](/blueprints/v2/reference/schema)
